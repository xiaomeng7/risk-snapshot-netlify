# Netlify 文件未更新 - 解决方法

## 🔴 问题：Netlify 上的文件没有更新

## ✅ 可能的原因和解决方法

### 原因 1: Netlify 没有连接到 GitHub（最常见）

**检查方法**：
1. 访问：https://app.netlify.com
2. 点击你的站点
3. 查看 "Site settings" → "Build & deploy" → "Continuous Deployment"
4. **检查是否显示 "Connected to Git"**

**解决方法**：
1. 如果显示 "Not connected"：
   - 点击 "Link to Git provider"
   - 选择 GitHub
   - 授权 Netlify 访问
   - 选择仓库：`xiaomeng7/risk-snapshot-netlify`
   - 点击 "Save"

2. 配置部署设置：
   - **Build command**: 留空（这是静态网站）
   - **Publish directory**: `.`（点号，表示根目录）
   - 点击 "Deploy site"

### 原因 2: Netlify 自动部署被禁用

**检查方法**：
1. Site settings → Build & deploy → Continuous Deployment
2. 查看 "Deploy settings"

**解决方法**：
1. 确保 "Auto publish" 是启用状态
2. 如果禁用了，点击启用

### 原因 3: 部署失败

**检查方法**：
1. 在 Netlify 控制台，点击你的站点
2. 查看 "Deploys" 标签
3. 查看最新的部署状态

**解决方法**：
1. 如果显示 "Failed"（失败）：
   - 点击失败的部署
   - 查看错误日志
   - 根据错误信息修复

2. 常见错误：
   - **Build command 错误**：确保 Build command 留空
   - **Publish directory 错误**：确保是 `.`（点号）

### 原因 4: Git 提交没有推送到 GitHub

**检查方法**：
```bash
cd /Users/mengzhang/Downloads/risk-snapshot-netlify
git status
git log --oneline -5
```

**解决方法**：
1. 如果有未提交的更改：
   ```bash
   git add .
   git commit -m "Update files"
   git push origin main
   ```

2. 确认推送到 GitHub：
   - 访问：https://github.com/xiaomeng7/risk-snapshot-netlify
   - 确认最新提交已显示

### 原因 5: Netlify 需要手动触发部署

**解决方法**：
1. 在 Netlify 控制台，点击你的站点
2. 点击 "Deploys" 标签
3. 点击 "Trigger deploy" → "Deploy site"
4. 等待部署完成

## 🔧 完整解决步骤（推荐）

### 步骤 1: 确认代码已提交到 Git

```bash
cd /Users/mengzhang/Downloads/risk-snapshot-netlify

# 检查状态
git status

# 如果有未提交的更改
git add .
git commit -m "Update files"
git push origin main
```

### 步骤 2: 确认代码已推送到 GitHub

1. 访问：https://github.com/xiaomeng7/risk-snapshot-netlify
2. 确认最新提交已显示
3. 点击最新提交，确认文件已更新

### 步骤 3: 检查 Netlify 连接

1. 访问：https://app.netlify.com
2. 点击你的站点
3. Site settings → Build & deploy → Continuous Deployment
4. **如果显示 "Not connected"**：
   - 点击 "Link to Git provider"
   - 选择 GitHub 并授权
   - 选择仓库：`xiaomeng7/risk-snapshot-netlify`
   - 配置：
     - Build command: （留空）
     - Publish directory: `.`
   - 点击 "Save"

### 步骤 4: 手动触发部署

1. 在 Netlify 控制台，点击你的站点
2. 点击 "Deploys" 标签
3. 点击 "Trigger deploy" → "Deploy site"
4. 等待部署完成（通常 1-2 分钟）

### 步骤 5: 验证部署

1. 部署完成后，访问你的网站
2. 右键 → "查看页面源代码"
3. 搜索 `G-QBYBDVL5FT`
4. **应该能找到 3 处**，说明部署成功

## 📋 检查清单

- [ ] Git 代码已提交
- [ ] Git 代码已推送到 GitHub
- [ ] GitHub 上能看到最新提交
- [ ] Netlify 已连接到 GitHub
- [ ] Netlify 部署设置正确（Build command 留空，Publish directory 是 `.`）
- [ ] Netlify 自动部署已启用
- [ ] 最新部署状态是 "Published"（已发布）
- [ ] 网站源代码显示最新代码

## 🚀 快速修复（如果已连接 GitHub）

如果 Netlify 已经连接到 GitHub，但文件没更新：

1. **手动触发部署**：
   - Netlify 控制台 → Deploys → Trigger deploy → Deploy site

2. **或者等待自动部署**：
   - 通常推送代码后 1-2 分钟会自动部署
   - 如果超过 5 分钟还没部署，手动触发

3. **检查部署日志**：
   - 点击部署记录
   - 查看是否有错误

## ⚠️ 常见问题

### Q: Netlify 显示 "Deploy failed"

**A**: 
1. 查看部署日志
2. 检查 Build command 是否留空
3. 检查 Publish directory 是否是 `.`

### Q: 代码已推送但 Netlify 没有自动部署

**A**:
1. 检查 Netlify 是否连接到 GitHub
2. 检查自动部署是否启用
3. 手动触发一次部署

### Q: 部署成功但网站还是旧版本

**A**:
1. 清除浏览器缓存（Cmd+Shift+Delete）
2. 强制刷新（Cmd+Shift+R）
3. 检查部署的 URL 是否正确

## 💡 推荐设置

为了确保 Netlify 正常工作，建议：

1. **连接 GitHub**（自动部署）
2. **Build command**: 留空
3. **Publish directory**: `.`
4. **Auto publish**: 启用

这样每次推送代码到 GitHub，Netlify 会自动重新部署。
